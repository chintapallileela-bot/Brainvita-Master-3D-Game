
import React from 'react';
import { Theme } from '../types';

interface RemovedMarblesProps {
  count: number;
  theme: Theme;
}

// This component is currently hidden in the new UI design.
export const RemovedMarbles: React.FC<RemovedMarblesProps> = () => {
  return null;
};
